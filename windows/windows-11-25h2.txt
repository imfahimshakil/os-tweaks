# Chocolatey (Run Powershell as Admin)
Set-ExecutionPolicy Bypass -Scope Process -Force;iex(irm https://community.chocolatey.org/install.ps1)

# Scoop
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser;iex(irm https://get.scoop.sh)

# PowerShell
winget install -e --id Microsoft.PowerShell

# Bitwarden
winget install -e --id Bitwarden.Bitwarden

# PeaZip
winget install -e --id Giorgiotani.Peazip

# Avro
winget install -e --id OmicronLab.Avro

# UltraViewer
winget install -e --id DucFabulous.UltraViewer

# Git
winget install -e --id Git.Git

# Firefox
winget install -e --id Mozilla.Firefox

# OBS
winget install -e --id OBSProject.OBSStudio

# Lightshot
# Turn off snipping tool
#   Settings -> Accessibility -> Keyboard:
#   Use the Print screen key to open screen capture -> Off
winget install -e --id Skillbrains.Lightshot

# PuTTY
winget install -e --id PuTTY.PuTTY

# BleachBit
sudo choco install bleachbit.install -y

# MKVToolNix
winget install -e --id MoritzBunkus.MKVToolNix

# MediaInfo
winget install -e --id MediaArea.MediaInfo.GUI

# Notepad++
winget install -e --id Notepad++.Notepad++
# Make notepadd++ default program for .txt file format
# Notepad++ (Run as Adminstrator) -> Settings:
#   File Association -> Notepad -> .txt -> Click "->"

# Internet Download Manager
sudo choco install openssl -y
winget install -e --id Tonec.InternetDownloadManager
# Activation
"U2FsdGVkX1/iIw2IcNvJFSG/Gt/+1Bu1AeymQyQbh+7AOO/z1ljex7CX1dPNp9wu+PSbx+Uz3Vaj+p8FBipoiA==" | openssl enc -aes-256-cbc -a -A -d -pbkdf2

# Revo Uninstaller Pro
# Download -> extract -> install -> follow readme.txt
# https://mega.nz/file/IQJF0T5K#yAZbaFVLsfGUUt1xEPlipvjHssImSa2wubSTzyzsaiw

# ImgBurn
winget install -e --id LIGHTNINGUK.ImgBurn

# Cloudflare WARP
winget install -e --id Cloudflare.Warp

# Bijoy Bayanno
# Download -> extract -> install (old requires dotnet 3.5) -> activate
# https://mega.nz/file/AUQ1TSBJ#iK_JXDUbgyGwHEDfhxqQyeYxmZ2cPlfgVdfqgEQSynE

# Python 3.12
winget install -e --id Python.Python.3.12

# MPC-HC
winget install -e --id clsid2.mpc-hc
# ffmpeg
curl -LOJ https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-essentials.zip; tar -xf ffmpeg-release-essentials.zip; ren (gci -Directory | ? Name -like 'ffmpeg*') ffmpeg; saps pwsh -v RunAs -a '-NoProfile -C "mv ffmpeg\bin\ffmpeg.exe ''C:\Program Files\MPC-HC''"'
rm .\ffmpeg, .\ffmpeg-release-essentials.zip -Recurse -Force
# yt-dlp
curl -L -o yt-dlp.exe https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp.exe; saps pwsh -v RunAs -a '-NoProfile -C "mv yt-dlp.exe ''C:\Program Files\MPC-HC'' -Force"'

# Canon CanoScan LiDE 120
# Download -> install
# https://www.canon-europe.com/support/consumer/products/scanners/canoscan/lide-series/canoscan-lide-120.html?type=drivers&language=EN&os=Windows%2011

# OpenVPN
winget install -e --id OpenVPNTechnologies.OpenVPN

# AOMEI Partition Assistant
# Download -> extract -> follow readme.txt -> install
# https://mega.nz/file/8FJRVJhJ#TOAWxVuQmS9SuNn5jGi5r0uGAQearuyzjCYUov_pvok

# VisualCppRedist AIO
scoop bucket add extras
sudo scoop install vcredist-aio

# MPV Player
sudo choco install mpvio.install -y

# SFVIP-Player
# Download -> extract ("C:\")
# Create a shortcut of "sfvip player.exe"
# Move the shortcut to the Start Menu folder:
#   Press Win + R
#   Type: shell:programs
#   Paste the shortcut into the folder that opens
#   (Optional) Rename the shortcut to a clean app name -> SFVIP-Player
https://mega.nz/file/MAhWGTwL#yPeowJXM3gZQAv5K51qsM23-dbj7j0RX2l7tKE1_uqQ

# Aria2
winget install -e --id aria2.aria2

# EaseUS Todo Backup
sudo choco install todobackup -y --ignore-checksums

# Winhance
scoop bucket add verymucho_knick-knacks https://github.com/verymucho/knick-knacks
scoop install winhance
# Import config.winhance

# Windhawk
sudo choco install windhawk -y

# Office 365 Enterprise
# Download -> extract -> install-office.bat
# https://mega.nz/file/cE51CTzT#TV1YnYpzrTsDNFekOap0gWSG8Y6UppdVv8qdhk5_miE

# Terminal Modification
# https://starship.rs/
# https://draculatheme.com/windows-terminal
